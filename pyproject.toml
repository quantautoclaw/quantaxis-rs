[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "quantaxis-rs"
version = "0.3.4"
description = "QUANTAXIS in Rust - High performance quantitative trading backtest engine"
requires-python = ">=3.8"
dependencies = []

[tool.maturin]
features = ["python"]